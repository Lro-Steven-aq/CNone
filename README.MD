## 我想要写一个 C Compiler.
现已实现了简单的词法分析器（然而，我并不想实现所有的C 标准的东西。因为懒或者麻烦）<br/>
我把注释处理放到了预处理（尽管我还没有开始写）

### 如何运行
1. 安装(Rust)[https://rust-lang.org/]
2. 然后在目录执行
   ```shell
    cargo run
   ```
   main.rs包含了3行C代码，控制台会输出删除注释的结果和词法分析的结果。