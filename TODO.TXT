编译器
想做一个C  编译器。
简单一些。

最后使用logos库


***********************************************************************
> 预处理没有实现，需要实现(include,if,define,else,ifdef,undef)
Lexer:                                                         词法分析
    1. 没有“.5”诸如的支持。（考虑）
    2. 转义支持\x,\u,\o
    无科学计数法和后缀支持（以后也不会！）
    \x41,\o,\u 支持不佳。（不计划）
    字符串前缀没有支持。（不计划）
    浮点数、整数精度问题：也许不一样。（不考虑）

# AST:
#     Root(Program)
#     |->FunctionDeclartion(ReturnType,ArgumentType)
    