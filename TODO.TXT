编译器
想做一个C  编译器。
简单一些。

最后使用logos库


***********************************************************************
Preprocessor:
    if else...没有实现。 include 不支持系统路径，define 不支持宏函数。
    #if 和 defined() 不计划支持。
Lexer:                                                         词法分析
    1. 没有“.5”诸如的支持。（考虑）
    2. 转义支持\x,\u,\o
    无科学计数法和后缀支持（以后也不会！）
    \x41,\o,\u 支持不佳。（不计划）
    字符串前缀没有支持。（不计划）
    浮点数、整数精度问题：也许不一样。（不考虑）

AST:
    没有函数声明/定义时类型占位的实现。
IR:
    todo!("更多的即将实现。。。(while, for, continue)"), （在generate_stmt(Stmt)函数里）
    is_filled() 是私有的，在函数is_block_filled()里面。
    todo!("更多的即将实现。。。(UnaryOp)"),   （在generate_expr(Expr)函数里的match分支的UnaryOp手臂中）
    _ => todo!("更多的即将实现。。。"),    （在generate_expr(Expr)函数里的match最后。）
    _ => todo!("更多的即将实现。。。（剩下5个位运算）"),  在generate_binary_operator(BinaryOp)函数里面的match最后的分支。
    
    